# rm -rf .venv
# uv venv
# source .venv/bin/activate && uv pip install -e ".[dev]"
vibe-blender generate "A coffee table" -v
# vibe-blender generate "A 3D object of a tree" -v
# vibe-blender generate "A simple red cube" -v